#include "algo.hpp"

Algo::Algo(
    vector<vector<int>> g) {
  // assign the graph
  // null check
  if(g == NULL) {
    // decide what to do?
    graph = g;
  }
  graph = g;
}

void Algo::PrimsSolve(){
    // pseudocode instructions
    
}